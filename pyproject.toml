[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "SirixDB"
authors = [{name = "Johannes Lichtenberger", email = "johannes.lichtenberger@sirix.io"}]
dynamic = ["version", "description"]
